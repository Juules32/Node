syntax = "proto3";
option go_package = "github.com/Juules32/Node";
import "google/protobuf/empty.proto";

package tokenring;

service TokenRing {
  rpc PassToken(Token) returns (google.protobuf.Empty) {}
}

message Token {
  // No fields needed
}

message Ack {
  bool success = 1;
}